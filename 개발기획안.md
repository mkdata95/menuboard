1. 프로젝트 개요
디지털 메뉴판 관리 시스템은 클라이언트별로 메뉴 디자인을 생성하고 관리할 수 있는 웹 기반 플랫폼입니다. 관리자는 각 클라이언트에 대해 지정된 시간에 맞춰 자동으로 메뉴판을 표시할 수 있도록 디자인을 설정하고 배포할 수 있습니다. 클라이언트는 메뉴판을 실시간으로 확인하며, 관리자는 편리한 인터페이스로 이를 제어할 수 있습니다.

2. 시스템 아키텍처
2.1 클라이언트 측
디지털 메뉴판 UI: 각 클라이언트가 볼 수 있는 메뉴판

디자인 슬라이드: 이미지, 텍스트, 가격 정보 등을 표시

자동 업데이트: 지정된 시간에 서버에서 제공된 최신 웹페이지를 자동으로 표시

배경 이미지 및 동영상: 메뉴판에 배경 이미지와 동영상을 표시

반응형 웹 디자인: 다양한 디스플레이 크기에 맞게 자동 조정

2.2 서버 측
API 서버: Node.js와 Express.js 기반의 서버

디자인 관리 API: 클라이언트에서 요청한 디자인 데이터를 처리

스케줄 관리 API: 각 클라이언트에 대해 지정된 시간에 메뉴판을 자동으로 업데이트

디자인 웹페이지 변환: 저장된 디자인을 웹페이지 형태로 변환하여 클라이언트에 제공

클라이언트 관리 API: 클라이언트 등록, 삭제 및 스케줄 설정

2.3 데이터베이스
MySQL 또는 MongoDB: 디자인, 클라이언트 정보 및 스케줄 관리

디자인 테이블: 각 디자인의 세부 정보, 상태(배경 이미지, 텍스트, 가격 정보 등)

클라이언트 테이블: 클라이언트 정보 및 연결된 디자인 목록

스케줄 테이블: 각 클라이언트별 디자인 표시 시간 관리

2.4 웹소켓/푸시 알림
클라이언트는 서버와 연결된 상태에서 웹소켓을 통해 실시간으로 업데이트된 디자인 정보를 받아올 수 있습니다.

3. 기능 설계
3.1 클라이언트 관리 시스템
클라이언트 등록/관리

관리자는 클라이언트 목록을 볼 수 있고, 각 클라이언트를 추가/삭제할 수 있음.

각 클라이언트에는 고유한 ID와 이름, 표시될 메뉴판 URL이 설정됨.

스케줄 설정

관리자는 각 클라이언트에 대해 메뉴판을 표시할 시간대와 디자인을 설정할 수 있음.

스케줄은 주간 또는 특정 날짜를 기반으로 설정 가능.

클라이언트는 지정된 시간에 자동으로 디자인을 불러와 표시.

3.2 디자인 관리 시스템
디자인 생성 및 관리

디자인 에디터: UI 기반으로 배경 이미지, 텍스트, 가격 정보, 동영상 등을 배치하는 UI 제공.

디자인 저장: 사용자는 디자인을 작성하고 저장하면, 서버에서 이를 웹페이지 형태로 변환하여 제공.

디자인 목록: 저장된 모든 디자인 목록을 관리자가 볼 수 있음.

디자인 수정 및 삭제: 이미 저장된 디자인을 수정하거나 삭제할 수 있음.

3.3 서버 측 API
디자인 저장 API: 사용자가 만든 디자인을 서버에 저장하고, 웹페이지로 변환.

클라이언트 목록 조회 API: 모든 등록된 클라이언트를 조회.

디자인 배포 API: 특정 디자인을 클라이언트에게 배포하고, 지정된 시간에 맞춰 표시.

스케줄 관리 API: 각 클라이언트에 대해 디자인을 표시할 시간 및 스케줄을 관리.

디자인 변환 API: 저장된 디자인을 웹페이지 주소로 변환하여 클라이언트가 접근할 수 있도록 제공.

3.4 실시간 업데이트 및 자동 갱신
웹소켓 기반 실시간 업데이트

클라이언트는 웹소켓을 통해 실시간으로 메뉴판을 업데이트 받을 수 있음.

서버에서 디자인 변경 시 해당 클라이언트에 즉시 반영됨.

4. 서버 디자인 및 API 설계
4.1 서버 엔드포인트
POST /design/create

설명: 새 디자인을 생성하여 서버에 저장하고, 웹페이지 주소로 변환하여 반환.

입력: 디자인 요소(배경 이미지, 텍스트, 가격 정보 등)

출력: 웹페이지 URL

GET /designs

설명: 저장된 모든 디자인 목록을 조회.

출력: 디자인 목록(디자인 ID, 웹페이지 URL 등)

POST /client/create

설명: 새 클라이언트를 등록.

입력: 클라이언트 이름, ID 등

출력: 클라이언트 등록 정보

GET /clients

설명: 모든 등록된 클라이언트를 조회.

출력: 클라이언트 목록

POST /schedule/set

설명: 클라이언트에 대해 디자인을 표시할 시간을 설정.

입력: 클라이언트 ID, 디자인 ID, 시작 시간, 종료 시간 등

출력: 스케줄 설정 결과

GET /schedule/{clientId}

설명: 특정 클라이언트에 대해 설정된 스케줄을 조회.

출력: 클라이언트의 스케줄 정보

GET /design/{designId}/url

설명: 특정 디자인의 웹페이지 URL을 조회.

출력: 웹페이지 URL

POST /client/update

설명: 클라이언트의 메뉴판을 갱신.

입력: 클라이언트 ID, 새로운 디자인 URL 등

출력: 클라이언트 갱신 결과

5. 데이터베이스 설계
5.1 디자인 테이블
designs

id (PK)

name (디자인 이름)

background_image (배경 이미지 URL)

text_content (텍스트 내용)

price_info (가격 정보)

video_url (동영상 URL)

created_at (디자인 생성 날짜)

updated_at (디자인 수정 날짜)

5.2 클라이언트 테이블
clients

id (PK)

name (클라이언트 이름)

client_url (클라이언트 메뉴판 URL)

created_at (클라이언트 등록 날짜)

updated_at (클라이언트 수정 날짜)

5.3 스케줄 테이블
schedules

id (PK)

client_id (FK: clients 테이블)

design_id (FK: designs 테이블)

start_time (디자인 표시 시작 시간)

end_time (디자인 표시 종료 시간)

created_at (스케줄 등록 날짜)

updated_at (스케줄 수정 날짜)

6. 시스템 흐름
디자인 관리

관리자는 디자인 생성 버튼을 눌러 디자인 에디터에 진입.

디자인을 작성하고 저장 후 서버에서 웹페이지 주소를 반환.

저장된 디자인은 디자인 목록에 추가되며, 클라이언트에 제공.

클라이언트 관리 및 스케줄 설정

관리자는 클라이언트 등록 후, 각 클라이언트에 대해 디자인 스케줄을 설정.

지정된 시간에 클라이언트는 해당 디자인의 웹페이지 주소를 받아 메뉴판을 표시.

자동 업데이트

클라이언트는 웹소켓을 통해 디자인 변동 사항을 실시간으로 수신.

클라이언트의 메뉴판은 자동으로 최신 디자인으로 갱신됨.

7. 개발 일정
1단계: 시스템 설계 및 환경 구축 (2주)

전체 아키텍처 설계, 프레임워크 및 라이브러리 선택

데이터베이스 설계 및 서버 기본 환경 구축

기본 API 설계

2단계: 디자인 관리 시스템 구현 (3주)

디자인 에디터 기능 개발 (Fabric.js 활용)

디자인 저장 및 웹페이지 변환 기능 구현

디자인 목록 페이지 구현

3단계: 클라이언트 관리 및 스케줄 설정 (3주)

클라이언트 등록/관리 기능 구현

스케줄 설정 및 자동 업데이트 기능 개발

4단계: 서버 API 구현 및 통합 (3주)

디자인, 클라이언트, 스케줄 API 개발 및 통합

웹소켓을 이용한 실시간 자동 갱신 기능 구현

5단계: 최종 테스트 및 배포 (2주)

통합 테스트 및 최종 디버깅

클라이언트와 서버 간의 데이터 통신 점검

실제 클라이언트 환경에서 테스트 후 배포

8. 기대 효과
효율적인 메뉴판 관리: 관리자가 한 번만 디자인을 설정하면 클라이언트는 자동으로 최신 메뉴판을 표시

사용자 편의성: 관리자는 직관적인 웹 UI를 통해 디자인과 클라이언트 관리, 스케줄 설정을 할 수 있음

실시간 업데이트: 클라이언트는 실시간으로 변경된 메뉴판을 확인할 수 있음

이 계획안은 디지털 메뉴판 관리 시스템을 개발하는 데 필요한 전반적인 방향을 제공합니다.









✅ 디지털 메뉴판 안드로이드 플레이어 앱 개발 기획안
📱 1. 앱 목적
이 앱은 서버에서 전달받은 디자인 웹페이지 URL을 전체화면으로 로딩하여, 디지털 메뉴판을 자동으로 표시하는 전용 플레이어입니다.
앱은 터치 없이 자동으로 실행되며, 네트워크를 통해 서버의 지시에 따라 실시간으로 콘텐츠를 교체합니다.

🔧 2. 주요 기능
✅ 앱 구동 로직
앱 실행 시:

내부 저장소에 저장된 서버 정보 및 클라이언트 ID 로드

서버에 현재 시간과 클라이언트 ID 전송 (GET /api/active-design?client_id=xxx)

응답 받은 디자인 URL 로딩

일정 주기(예: 1분)로 서버에 재요청하여 디자인 URL 변경 시 자동 반영

✅ 전체화면 웹뷰
시스템 UI (상단바, 네비게이션바) 모두 숨김

스크린세이버 방지 (항상 화면 켜짐 유지)

HTML 콘텐츠는 캐시 없이 항상 새로고침

오류 발생 시 대체 화면 제공 ("연결 오류", "디자인 오류")

✅ 설정 메뉴 (숨겨진 관리자 기능)
화면 중앙을 3초 이상 터치 시 설정 창 팝업

설정 항목:

서버 주소 (http://192.168.0.2:8000)

클라이언트 ID (easysign001)

접속 확인 버튼

저장 후 재시작

✅ 저장소
SharedPreferences를 통해 앱 설정값 저장

server_ip

client_id

auto_reload_interval

✅ 기타 기능
부팅 시 자동 실행 (BOOT_COMPLETED 리시버 등록)

인터넷 미연결 또는 서버 오류시 자동 재시도

추후 확장: 디바이스 상태 서버로 전송

💾 3. 화면 구성
▶ 기본 화면
웹뷰 전체화면 (디자인 URL 표시)

에러 발생 시 텍스트 오버레이

▶ 설정 팝업 화면
AlertDialog 형식의 팝업

서버 주소 입력

클라이언트 ID 입력

저장 및 테스트 버튼

🔐 4. 보안 및 안정성
서버와 통신 시 ID 포함 (간단한 인증 수준)

웹뷰의 외부 JS 차단

URL 강제 지정 방지 (외부에서 접근 시 무효화)

재부팅 후에도 앱 자동 실행

🛠️ 5. 기술 스택 및 환경
항목	기술
Android 최소 버전	7.1 (API 25)
언어	Java (또는 Kotlin 가능)
뷰	WebView
저장	SharedPreferences
백그라운드 처리	Handler / WorkManager
에러 처리	try-catch + fallback 화면
UI 라이브러리	Android 기본 AlertDialog 등 최소화

🧪 6. 테스트 시나리오
서버 연결 테스트

웹뷰 로딩 속도 확인

3초 터치로 관리자 모드 진입 확인

스케줄 변경 시 URL 변경 확인

서버 다운 시 에러 메시지 노출 여부

📦 7. 향후 추가 기능 제안
서버와 지속적인 상태 동기화 (Ping)

앱에서 직접 로그 전송 (장애 모니터링)

OTA 업데이트 기능

미디어 파일 캐싱 및 오프라인 모드